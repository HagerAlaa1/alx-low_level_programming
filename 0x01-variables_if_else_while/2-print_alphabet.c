#include <stdio.h>
/**
 * main - the main function
 *Return: 0
 */
int main(void)
{
	char Alphabet;

	for (Alphabet = 'a'; Alphabet <= 'z'; Alphabet++)
		putchar(Alphabet);
		putchar('\n');

	return (0);
}
